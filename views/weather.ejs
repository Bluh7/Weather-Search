<%- include('partials/header') %>
<body>
  <div class="container">
    <div class="row">
      <div class="col-12" style="margin-top: 40vh;">
        <form action="/search" method="post">
          <div class="text-center" style="margin-bottom: 1em;">
            <% if(status == 200) { %>
              <% if (cityState) { %>
                <h1><%= city %>, <%= cityState %> <%= temperature %>C°<img crossorigin="anonymous" src="<%= iconUrl %>"></h1>
              <% } else { %>
                <h1><%= city %> <%= temperature %>C°<img crossorigin="anonymous" src="<%= iconUrl %>"></h1>
              <% } %>
              <% if (temperature != feelsLike) { %>
                <label>Current weather situation is <%= description %> and feeling like <%= feelsLike %>C°</label>
              <% } else { %>
                <label>Current weather situation is <%= description %></label>
              <% } %>
            <% }else{ %>
              <h1>City not found 🌎</h1>
              <label>Created by <a href="https://github.com/Bluh7" target="_blank" style="color: #fff;">Blu</a>, CSS developed by monkeys 🐒</label>
            <% } %>
          </div>
          <div class="input-group-text mb-3">
            <input type="text" class="form-control" placeholder="Search City" name="city">
            <button type="submit" class="btn">
              <span class="input-group-text">
                <svg xmlns="http://www.w3.org/2000/svg" width="19" height="19" fill=#243b55 class="bi bi-search" viewBox="0 0 16 16">
                  <path d="M11.742 10.344a6.5 6.5 0 1 0-1.397 1.398h-.001c.03.04.062.078.098.115l3.85 3.85a1 1 0 0 0 1.415-1.414l-3.85-3.85a1.007 1.007 0 0 0-.115-.1zM12 6.5a5.5 5.5 0 1 1-11 0 5.5 5.5 0 0 1 11 0z"/>
                </svg>
              </span>
            </button>
          </div>
        </form>
      </div>
  </div>
</body>
<%- include('partials/footer') %>